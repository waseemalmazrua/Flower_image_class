services:
  - type: web
    name: flower-classifier-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py --server.port=10000"
    envVars:
      - key: STREAMLIT_SERVER_PORT
        value: "10000"
